module.exports = async (req, res) => { try { const raw = process.env.FIREBASE_SERVICE_ACCOUNT; const json = JSON.parse(raw); res.status(200).json({ projectId: json.project_id }); } catch(e) { res.status(200).json({ error: e.message }); } };
